<!-- 改编自：职业法律手工艺主题项目提示词/cy-402/cy-402.md；后端由 Express + TypeScript 改造为 Go 1.22 + Gin + GORM -->
# cy-402 律师案件管理系统

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**律师案件管理系统（cylawcase）** 面向律师事务所，支持客户管理、案件全流程跟踪、文档归档和费用结算的一体化管理。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户/律师）**
  - 字段：id, username, password_hash, real_name, role（admin/lawyer/assistant）, license_no, email, phone, avatar, created_at
  - 关键关系：可主办多个 Case、上传多个 Document、创建多个 Billing
  - 业务动作：登录/注册、修改资料、律师分配
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/repository/user_repository.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/userStore.ts → frontend/src/pages/Profile.tsx**

- **Client（客户）**
  - 字段：id, name, id_number, contact, address, remark, created_at
  - 关键关系：拥有多个 Case；被多个 Billing 引用
  - 业务动作：新建客户、编辑信息、检索客户、查看历史案件
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **Case（案件）**
  - 字段：id, case_no, title, case_type（civil/criminal/administrative/commercial/labor）, status（filed/investigating/hearing/closed/archived）, accept_date, close_date, summary, client_id, lead_lawyer_id, co_lawyer_ids（JSON）, created_at
  - 关键关系：隶属于 Client，由主办律师（User.lawyer）负责，关联多个 Document 和 Billing
  - 业务动作：创建案件、状态流转、分配律师、筛选查询
  - **此实体必须贯穿全栈**

- **Document（文档）**
  - 字段：id, title, file_type（complaint/defense/evidence/judgment/contract/other）, file_url, upload_time, case_id, uploader_id, created_at
  - 关键关系：隶属于 Case，由 User 上传
  - 业务动作：上传文档、按案件查看、下载、删除
  - **此实体必须贯穿全栈**

- **Billing（费用账单）**
  - 字段：id, bill_no, billing_type（attorney_fee/court_fee/travel_fee/other）, amount, status（pending/paid/invoiced/void）, case_id, client_id, invoice_info, created_at
  - 关键关系：隶属于 Case，引用 Client
  - 业务动作：创建账单、标记支付、开票、汇总统计
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/cases（案件列表）**
  - 消费实体：Case、User
  - 关键交互：表格展示、按类型/状态/律师/时间范围筛选
  - 共享组件：`<CaseCard>`、`<FilterBar>`、`<StatusBadge>`

- **/cases/:id（案件详情）**
  - 消费实体：Case、Client、Document、Billing、User
  - 关键交互：案件信息、关联客户、文档列表、账单列表、时间线
  - 共享组件：`<DocumentList>`、`<TimelineItem>`、`<StatusBadge>`（与 /cases 共用）、`<AmountSummary>`

- **/clients（客户管理）**
  - 消费实体：Client、Case
  - 关键交互：客户列表 + 详情侧边栏、历史案件查看
  - 共享组件：`<ClientCard>`、`<CaseTable>`

- **/billing（费用中心）**
  - 消费实体：Billing、Case、Client
  - 关键交互：账单列表、按案件/客户/状态筛选、本月应收/已收/待收汇总
  - 共享组件：`<BillingCard>`、`<AmountSummary>`（与 /cases/:id 共用）、`<StatusBadge>`

- **/documents（文档中心）**
  - 消费实体：Document、Case
  - 关键交互：按案件分组列表、文件类型筛选、全文搜索
  - 共享组件：`<DocumentCard>`、`<FileUploader>`、`<DocumentList>`（与 /cases/:id 共用）

- **/profile（个人中心）**
  - 消费实体：User
  - 关键交互：律师资料维护、头像上传
  - 共享组件：`<AvatarUploader>`

### 4. 横切关注点（≥ 2 个）

- **角色权限控制（JWT + RBAC）**
  - 触达文件层：
    - 数据库：`users` 表含 role 字段
    - 后端：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中律师/管理员专属接口需 `AuthRequired()` 和 `RequireRole()`
    - 前端：`router/guards.tsx`、`components/common/PermissionGuard.tsx`、`stores/authStore.ts`

- **操作日志**
  - 触达文件层：
    - 数据库：`audit_logs` 表（操作人、操作类型、目标实体、变更内容 JSON）
    - 后端：`internal/middleware/audit_log.go`、`internal/constants/log_templates.go`
    - 前端：`/audit-logs` 审计日志查看页面

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - 前端：`utils/request.ts` 拦截器

- **文件上传**
  - 触达文件层：
    - 后端：`internal/handler/upload_handler.go`、`internal/util/file_upload.go`
    - 前端：`components/common/FileUploader.tsx`
    - 配置：`internal/config/config.go`（上传大小与类型限制）

### 5. 共享枚举/常量（≥ 2 组）

- **CaseStatus（案件状态）**
  - 值：FILED = 'filed', INVESTIGATING = 'investigating', HEARING = 'hearing', CLOSED = 'closed', ARCHIVED = 'archived'
  - 前端定义位置：`frontend/src/constants/case.ts`
  - 后端定义位置：`backend/internal/constants/case.go`
  - 出现位置：Case 模型、CaseService、StatusBadge、案件列表筛选、日志模板、错误码、formatters
  - **README 必须列出该枚举在前后端的所有出现位置**

- **BillingType（费用类型）**
  - 值：ATTORNEY_FEE = 'attorney_fee', COURT_FEE = 'court_fee', TRAVEL_FEE = 'travel_fee', OTHER = 'other'
  - 前端定义位置：`frontend/src/constants/billing.ts`
  - 后端定义位置：`backend/internal/constants/billing.go`
  - 出现位置：Billing 模型、BillingService、费用中心筛选、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **BillingStatus（账单状态）**
  - 值：PENDING = 'pending', PAID = 'paid', INVOICED = 'invoiced', VOID = 'void'
  - 前端定义位置：`frontend/src/constants/billing.ts`
  - 后端定义位置：`backend/internal/constants/billing.go`
  - 出现位置：Billing 模型、BillingService、StatusBadge、AmountSummary、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<CaseCard>`：被 /cases 和 /clients 共用
- `<DocumentList>`：被 /cases/:id 和 /documents 共用
- `<StatusBadge>`：被 /cases、/cases/:id 和 /billing 共用
- `<TimelineItem>`：被 /cases/:id 使用
- `<AmountSummary>`：被 /billing 和 /cases/:id 共用
- `<ClientCard>`：被 /clients 使用
- `<CaseTable>`：被 /clients 使用
- `<BillingCard>`：被 /billing 使用
- `<DocumentCard>`：被 /documents 使用
- `<FileUploader>`：被 /documents 使用
- `<FilterBar>`：被 /cases 使用
- `<AvatarUploader>`：被 /profile 使用
- `<PermissionGuard>`：被多个页面用于按钮/区块权限控制

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useAuth()`：JWT 认证、角色信息获取、权限判断
- `usePagination()`：列表分页逻辑复用
- `useFileUpload()`：文档上传逻辑封装（进度、校验、多文件）
- `usePermission()`：按钮/区块权限判断
- `utils/dateFormat.ts`：日期时间格式化
- `utils/amountFormatter.ts`：金额格式化
- `utils/request.ts`：Axios 请求封装与错误拦截

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户角色校验权限
- `rate_limiter.go`：接口限流
- `error_handler.go`：统一错误响应格式
- `audit_log.go`：操作日志记录
- `cors.go`：跨域配置

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`cylawcase`
- **前端端口**：28031
- **后端端口**：29069
- **数据库端口**：38102
- **前端框架**：React 18 + TypeScript
- **UI 库**：Ant Design
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL 16
- **ORM**：GORM
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/postgres`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # auth.ts, user.ts, client.ts, case.ts, document.ts, billing.ts
├── stores/           # authStore.ts, userStore.ts, clientStore.ts, caseStore.ts, documentStore.ts, billingStore.ts
├── types/            # 共享类型定义
├── components/common/# CaseCard, DocumentList, StatusBadge, TimelineItem, AmountSummary, ClientCard, CaseTable, BillingCard, DocumentCard, FileUploader, FilterBar, AvatarUploader, PermissionGuard
├── hooks/            # useAuth.ts, usePagination.ts, useFileUpload.ts, usePermission.ts
├── pages/            # Cases, CaseDetail, Clients, Billing, Documents, Profile, AuditLogs
├── router/           # index.tsx + guards.tsx
├── utils/            # dateFormat.ts, amountFormatter.ts, request.ts
└── constants/        # case.ts, billing.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（数据库连接、JWT、限流、端口、上传限制等）
    ├── model/                   # 按实体分文件：user.go, client.go, case.go, document.go, billing.go
    ├── repository/              # 按实体分文件：user_repository.go, client_repository.go, case_repository.go, document_repository.go, billing_repository.go
    ├── service/                 # 按实体分文件：user_service.go, client_service.go, case_service.go, document_service.go, billing_service.go
    ├── handler/                 # 按实体分文件：user_handler.go, client_handler.go, case_handler.go, document_handler.go, billing_handler.go, upload_handler.go
    ├── router/                  # router.go + 按实体分文件：users.go, clients.go, cases.go, documents.go, billings.go
    ├── middleware/              # auth.go, rbac.go, rate_limiter.go, error_handler.go, audit_log.go, cors.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # case.go, billing.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, formatters.go, app_error.go, amount_formatter.go, file_upload.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `CASE_STATUS_CHANGE_SUCCESS`、`BILLING_PAID_FAILED`
   - 任何业务字段变更（如 Case 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"Case[id=123] assign failed: lawyer not match"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、金额格式化、状态文本、案件类型文本、账单类型文本
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **案件状态机必须跨多处定义**
   - CaseStatus 的流转规则在 service 状态机、前端按钮显隐、日志模板、错误码、formatters 中同时存在
   - 新增一个状态值需要修改至少 10 处

5. **枚举必须前后端重复定义且被多处引用**
   - CaseStatus、BillingType 和 BillingStatus 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 Case 新增一个 'appeal' 案件类型"，必须触达：GORM AutoMigrate、Case 模型、前后端 constants、CaseService、CaseCard、FilterBar、formatters、日志模板、错误码、案件列表筛选逻辑等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=cylawcase`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: cylawcase`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-cylawcase}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `${FRONTEND_PORT:-28031}:80`，后端 `${BACKEND_PORT:-29069}:8080`，数据库 `${DB_PORT:-38102}:5432`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=cylawcase`
- 数据库连接相关：`DB_NAME=cylawcase_db`、`DB_USER=cylawcase_user`、`DB_PASSWORD=cylawcase_pwd`、`DB_ROOT_PASSWORD=cylawcase_root`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=28031`、`BACKEND_PORT=29069`、`DB_PORT=38102`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
frontend/coverage/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 CaseStatus、BillingType 和 BillingStatus 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:28031` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<StatusBadge>` 或 `<DocumentList>` 或 `<AmountSummary>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
